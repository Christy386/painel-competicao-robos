import {Box, Button} from '@mui/material/Box';

export default function Home() {
  return (
    <Box
      sx={{
        width: '100vw',
        height: '100vh',
        backgroundColor: '#fff',
        color: '#000',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'column'
      }}
    >
      <Button >

      </Button>
    </Box>
      
  )
}
