/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['eventos.ifrn.edu.br'], // Add any other allowed domains here if needed
  },
}

module.exports = nextConfig
